---
import type { Meme } from "../constants/memes";

export type Props = Meme;

const { title, description, img, alt, width } = Astro.props;
---

<h1>{title}</h1>
<img src={img} {width} {alt} />
<h1>{description}</h1>
